import type { Metadata } from "next";
import CreateProfilePage from "./create-profile-client";

export const metadata: Metadata = {
  title: "Create Profile",
  description: "Create a new trading profile on CryptoCompete to start a fresh portfolio.",
};

export default CreateProfilePage;
