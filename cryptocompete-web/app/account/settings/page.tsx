import type { Metadata } from "next";
import AccountSettingsPage from "./settings-client";

export const metadata: Metadata = {
  title: "Settings",
  description: "Manage your CryptoCompete account settings, email, and preferences.",
};

export default AccountSettingsPage;
