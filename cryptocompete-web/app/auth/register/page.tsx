import type { Metadata } from "next";
import RegisterPage from "./register-client";

export const metadata: Metadata = {
  title: "Sign Up",
  description:
    "Create a free CryptoCompete account and start practicing crypto trading with virtual money. No real money required.",
};

export default RegisterPage;
