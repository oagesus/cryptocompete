import type { Metadata } from "next";
import VerifyPage from "./verify-client";

export const metadata: Metadata = {
  title: "Verify Email",
  description: "Verify your CryptoCompete email address to activate your account.",
};

export default VerifyPage;
