import type { Metadata } from "next";
import LoginPage from "./login-client";

export const metadata: Metadata = {
  title: "Sign In",
  description:
    "Sign in to CryptoCompete and continue practicing crypto trading with virtual money. Track your portfolio and compete on the leaderboard.",
};

export default LoginPage;
